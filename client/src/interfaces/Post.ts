export interface Post {
    id: string;
    title: string
    smallText: string
    fullText: string
    imageURL: string
    createdAt: string
    group: string
    tags: string
    likesCount: string
    raiting: string
    userId: string
}